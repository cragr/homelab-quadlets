[Unit]
Description=Password Pusher Pod (app + gateway + MariaDB)

[Pod]
# Pod's name (systemd service will be pwpush-pod.service)
PodName=pwpush

# Public gateway (HTTP inside the pod on :80)
PublishPort={{GATEWAY_HOST_PORT}}:80

# Internal admin app (listens on :5100 inside the app container)
PublishPort={{APP_HOST_PORT}}:5100