[Container]
ContainerName=cloudflared
Image=docker.io/cloudflare/cloudflared:latest
Network=appnet

Exec=tunnel run --token INSERT_TOKEN_HERE

[Service]
Restart=always
RestartSec=2

[Install]
WantedBy=default.target